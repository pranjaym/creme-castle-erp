/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Region pinned to Mumbai in deploy config, colocated with the spine (sibling-app pattern).
};
export default nextConfig;
