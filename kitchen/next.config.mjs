/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Region pinned to Mumbai in deploy config, colocated with Supabase (sibling-app pattern).
};
export default nextConfig;
